i18n.merge_file(
    input: meson.project_name() + '.desktop.in',
    output: meson.project_name() + '.desktop',
    po_dir: join_paths(meson.source_root()),
    type: 'desktop',
    install: true,
    install_dir: join_paths(datadir, 'applications')
)

